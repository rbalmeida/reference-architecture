# Setup of postgres environment with UI admin pgadmin4
Run with:
```
docker compose up -d
```
Stop with:
```
docker compose down
```

## Admin web app
Open browser to http://localhost:5050

## db connection
Db connection available on the exposed port 5432
